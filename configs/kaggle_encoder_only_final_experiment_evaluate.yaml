inherits: configs/kaggle_encoder_only_final.yaml

evaluation:
  model_source: eat_checkpoint
  checkpoint_path: /kaggle/working/models/eat_bart_encoder_only_final
  output_path: /kaggle/working/reports/eat_bart_encoder_only_final_experiment_generations.csv
  max_eval_examples:
  batch_size: 4
  max_new_tokens: 160
  num_beams: 4
  do_sample: false
  repetition_penalty: 1.15
  no_repeat_ngram_size: 3
  length_penalty: 1.15
  early_stopping: true
